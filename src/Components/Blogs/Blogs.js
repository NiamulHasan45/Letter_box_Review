import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h2>Context api</h2>
            <p>React context api solved the problem of props drilling. If we previously wanted to get a data from grandparents to grandchild, we had to pass value with every consecutive child to pass it. Context api solved it. we can pass value by context api without passing in every stage of it .</p>
            <h2>Symantic Tag</h2>
            <p>Symantic tag describes the actual purpose of the coding part. Like article tag is used to describe a specific task. Same as table, form. It is introduced in HTML 5. It helps user to understand code easily.</p>

        </div>
    );
};

export default Blogs;